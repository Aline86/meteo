[functions]
  external_node_modules = ["express"]
  node_bundler = "esbuild"
[[redirects]]
  force = true
  from = "https/*"
 
  status = 200
  to = "http://localhost:3000/*"
[build]
  functions = "backend/server.js"

[dev]
  publish = "frontend"