[functions]
  external_node_modules = ["express"]
  node_bundler = "esbuild"
[[redirects]]
  force = true
  from = "http/*"
 
  status = 200
  to = "/backend/router/api.js"
[build]
  functions = "backend/:router"

[dev]
  publish = "frontend"